# Examples

Run:

```bash
python examples/risk_score_example.py
```

This builds the Section 8.2 worked patient scenario and prints the full
scoring result, matching the shape in `resources/sample_response.json`.
