# Usage

```python
from preventive_risk_engine import score_patient
result = score_patient(patient)
print(result.to_api_response())
```

See `examples/risk_score_example.py` for a full worked patient build.
